query GetSearchItems($limit: Float!, $searchQuery: String!) {
  searchItems(limit: $limit, searchQuery: $searchQuery) {
    id
    name
    img
    isFavourite
    itemId
    storePrices {
      storeName
      price
      saving
      discount
      storeId
    }
  }
}
