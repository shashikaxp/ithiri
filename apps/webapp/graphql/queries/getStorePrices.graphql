query GetStoreItems($limit: Float!, $offset: Float!) {
  getStoreItems(limit: $limit, offset: $offset) {
    id
    name
    img
    storePrices {
      storeName
      price
      saving
      discount
      storeId
    }
  }
}
