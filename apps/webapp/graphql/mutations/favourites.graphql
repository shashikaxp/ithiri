mutation Favourite($itemId: Float!) {
  favourite(itemId: $itemId) {
    id
    name
    img
    isFavourite
    itemId
    storePrices {
      storeName
      price
      saving
      discount
      storeId
    }
  }
}
